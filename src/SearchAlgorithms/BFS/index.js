function BFS(agentPosition) {
    const openList = []
    const closedList = []
    openList.push(agentPosition)
    let current = null
    while (openList.length > 0) {
        current = openList[0]
        openList.slice(1);
        [[1, 0], [0, 1], [-1, 0], [0, -1]].forEach(element => {

        })
    }
}

export default BFS;
